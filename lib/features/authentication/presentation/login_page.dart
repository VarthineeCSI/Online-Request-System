import 'package:flutter/material.dart';
import 'package:onlinerequestsystem/features/authentication/presentation/login_widget.dart';

class LoginPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      // body: LoginWidget(),
      body: LoginWidget(),
    );
  }
}
